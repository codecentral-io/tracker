{
  "name": "tracker",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "concurrently": "^3.6.1",
    "prettier": "^1.14.2"
  },
  "scripts": {
    "start": "concurrently --kill-others \"cd server && npm start\" \"cd client && npm start\""
  }
}
